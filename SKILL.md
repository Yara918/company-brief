---
name: company-brief
description: 会前公司速读：对目标公司做公开信息检索与整理，产出一份商务 HTML 公司简报（速读卡、公司快照、招聘信号、风险、会面话题）。当用户给出公司名或官网URL，需要会前准备、BD/销售拜访、商务谈判、客户/供应商评估时使用。触发词：会前情报、公司速读、公司调研、客户拜访、尽调、BD、sales prep、company research。
license: MIT
compatibility: 标准 Agent Skill 格式（WorkBuddy 已实测通过；Codex 待实测；Claude Code 接 DeepSeek 模型时聊天交付不可靠，建议接官方 Claude 或强模型）。模型联网自动检索（WebSearch），无联网工具时降级为用户贴回信源。脚本需 Python 3.10+。输出自包含 HTML。
metadata:
  author: <你的名字/ID>
  version: "1.0.0"
---

# company-brief · 会前公司速读

## Workflow

1. 收集必填输入（缺一不检索，一次性列全要什么）
2. **模型联网检索**（WebSearch 逐组查 G1-G7，见 references/reference.md），直接出报告，不用用户自己搜；环境无联网工具时降级为「出清单让用户贴回」
3. 登记信源（含真实 URL）
4. 填内容字段（按字数上限，查不到写「未检索到」）
5. 渲染 HTML（report-template.html）→ **运行 scripts/verify-links.py 机器质检**（链接真实性 / 敏感词 / KPI 数字 / 来源 / HTML 结构，任一不过 = FAIL）
6. → **在聊天中贴出校验脚本的原始输出**（[OK] 或 [FAIL] 清单逐行粘贴，禁止转述、禁止自称"已通过"）
7. 交付：只有脚本输出 [OK] 才交付，交付句 `📄 报告已生成：<路径>（机器校验通过）`；[FAIL] 禁止交付，必须按清单修正后重跑

## 输入（缺一不检索）

```
公司名称：
公司网址：（不给就授权我去搜候选网址给你确认；没有官网请明说）
会面目的：
我方业务：
```

- **必填四项**：公司名称 / 公司网址 / 会面目的 / 我方业务。缺一不检索，停下一次性列全要什么，等齐才开工。
- 网址三分支：给了 → 直接用，禁止再检索候选网址、禁止停下来确认；没给/说「你去搜」→ 检索候选网址列给用户确认；明确说「没有官网」→ 放行并如实标注。

**缺信息提示**（一次性列全，按以下话术对用户说）：
```
请补齐以下四项即可开工：
公司名称：
公司网址：（不给就授权我去搜候选网址给你确认；没有官网请明说）
会面目的：
我方业务：
```

## Phase 1: 检索（模型联网自动检索）

**流程**：模型按 G1-G7（见 references/reference.md）用联网搜索逐组检索（WebSearch），登记真实 URL，填内容，渲染。**不要求用户自己搜索。**

- 环境无联网工具时，降级为「出清单让用户贴回」，并在交付时说明用了降级模式。
- 不向用户解释内部机制，直接干活、给结果。

## 分层查询规则（详见 references/reference.md 的 A 节）

分层查询：社媒必查 5 + 按行业查；招聘必查 4+官网 + 按行业查。每条独立查询，命中写账号名+链接，查无写未检索到+检索记录。

## 内容字段（字数上限，查不到写「未检索到」）

| 字段 | 上限 |
|---|---|
| 公司一句话（one_liner） | ≤ 40 字 |
| 3 个关键数字（体量/采购/财务） | 各 ≤ 60 字，带来源 |
| 公司快照每行（定位/主体/融资/产品/竞品/客户） | 各 ≤ 80 字，带来源 |
| 招聘信号 | ≤ 80 字，带平台链接 |
| 风险每段（事实/依据/对我方） | 各 ≤ 50 字，带来源 |
| 会面话题（3 个） | 各 ≤ 60 字 |
| Fit 评估（fit_assessment / fit_reason） | 徽章 ≤ 10 字（强/中/弱）；理由 ≤ 80 字，不带来源 |
| 检索记录（search_log） | 平台×关键词×结果，每条 ≤ 40 字 |

- **Fit 评估放在会面话题内**：作为会面话题的最后一项（话题③之后），格式「Fit 评估：这单合作值不值得谈：<徽章 强/中/弱> <理由>」，**不带来源**（Fit 是综合判断，不引用单条链接）。
- **Fit 结论与理由必须一致**：结论「强」时理由要支撑强（机会明确）；结论「弱」时理由要说明不宜投入；**禁止「强」的结论配「刚起步/量有限/谨慎」的矛盾理由**——结论和理由方向必须一致。

- 3 个关键数字限定：① 体量（营收/员工/用户）② 规模（年采购额/供应商/在招采购岗）③ 财务健康（净利/毛利/融资）——禁止凑数，查不到写「未检索到」。
- **KPI 必须是数字（硬性）**：速读卡 3 个关键数字**每个都必须是大数字/数量**（如「95.6万粉」「在招采购岗3名」「净利率9%」）；**禁止填文字结论**（如「面包品类已跑通」「采购体系成熟」这类表述不得出现在 KPI 位置）——规模查不到确切数字时，用「在招采购岗 X 名」「供应商 X 家」「单品销售额 X 万」等数量表述，实在没有才写「未检索到」。
- **财务数字口径规则（硬性）**：
  - 必须用**最新报告期完整数据 + 同比**，禁止用孤立的半年度/单季度数字冒充全年；半年数据必须写明「XX 上半年」。
  - 必须核对**趋势**：营收/净利是增是减要写准确（如「净利 55.22 亿，同比 -1.77%」）；禁止只写利好数字、漏掉下滑事实。
  - 发现「增收不增利」「净利下滑」等负面信号，必须写进风险信号区，不得只放在 KPI。
  - 数字与来源不符时，以可核实的公开报道为准，宁写「未检索到确切数字」也不硬填。
- **KPI ② 规模必须有实质内容**：优先给数字（年采购额/供应商数量/在招采购岗数量），三者至少有一个；查不到确切数字时，给实质信号（在招采购岗名称+平台、已知供应商/合作方类型），禁止只写「未披露年采购额」就带过。
- **风险信号禁止模糊表述**：每段风险必须写明「具体事实 + 可核验来源」；写不清事实、来源对不上的内容宁可不写，禁止「另有媒体提及」「据相关报道」这类无法核验的表述。
- 查不到 = 如实写「未检索到」，绝不编造、绝不推测填充。

## 质量铁律

1. **真实采集**：每条信息有真实 URL，**来自本次联网检索结果**；禁止生成 URL、禁止凭记忆填充、禁止拼接或改写 URL。
   - **链接必须来自检索结果原文**：报告里每个链接必须是本次检索结果中真实出现的 URL；禁止根据域名/ID 格式推测编造、禁止拼接构造（如按账号 ID 格式造主页链接）。检索结果里没有真实链接的内容，不写该条，或换写有真实来源的内容。
   - **社媒主页链接必须有据可依**：贴「进入主页」按钮前，必须能对应到检索结果中真实出现的账号主页链接；检索结果中没有该平台主页链接时，该平台行写「未检索到账号主页」，禁止贴推测的链接。
2. **来源可点且文字可辨**：每个内容点的来源是美观的可点链接，**按钮文字必须描述来源**（如「官网·关于我们」「2025年报（巨潮）」「企查查」「BOSS直聘」），禁止统一写「来源」「来源1」「来源2」；禁止裸 URL、禁止纯文字「来源：xxx」。
3. **死链不进报告（渲染前必须自检）**：所有引用链接在渲染前逐条自检可访问性——打不开的（404/567/跳登录墙/403 拒绝）不引用；**登录墙/验证码/403 反爬页必须标注「需登录」或「访问受限」，优先选免登录来源**（官网/新闻/百科）；拿不准能否打开的链接宁可不引用，也不放进报告。
4. **来源不重复**：同一 URL 在报告中最多出现 2 次；**降级场景例外**：同一官网 URL 最多 3 次，且必须至少配 1 个非官网来源（新闻/百科），禁止全篇只用一个来源反复引用；超过上限必须换替代来源，或如实标注「以同一来源为准」。
5. **查不到如实写**：招聘/社媒/数据查不到 → 写「未检索到」+ 检索记录（平台×关键词→结果），证明真查过。
6. **敏感词零**：报告不含真人名/公司名/敏感词（违规词出现即不合格）。
7. **机器校验 + 聊天交付（硬门禁）**：渲染后必须运行 `python scripts/verify-links.py <报告> <目标实体>`，并在聊天中**原样粘贴脚本输出**（从 `[OK]`/`[FAIL]` 起逐行，禁止转述、禁止自称"已通过"）。**只有脚本输出为 `[OK] 全部通过` 才允许交付**，交付句 `📄 报告已生成：<路径>（机器校验通过）`；**脚本输出 [FAIL] 时禁止交付**，必须按 FAIL 清单逐条修正后重跑直至 [OK]（校验范围含链接 / 敏感词 / KPI / 来源 / HTML 结构）。浏览器自动弹出只是辅助，不是交付标准。
8. **渲染前逐项完整性自检（交付前必做，缺一不可）**：渲染完成后、交付前，逐项核对以下清单，全部通过才交付：
   - **每处内容点都有来源**：逐条检查所有正文内容（速读卡/快照每行/招聘/风险/话题/Fit），每条必须带可点来源链接；缺来源的内容点，要么补上来源，要么如实标注「未检索到出处」，禁止裸内容无来源。
   - **社媒行有来源或如实标注**：每个平台行必须有账号主页链接、或「未检索到」、或「仅见报道提及」三选一，禁止空白行。
   - **链接不重复**：数一遍全文每个 URL 出现次数，超上限（普通 2 次/降级场景官网 3 次）的先替换再交付。
   - **死链已排除**：所有链接渲染前已自检可访问性，无 404/567/403/登录墙未标注项。
   - 自检结果在交付句前用一行说明（如「已自检：来源完整 ✓ 链接无重复 ✓ 无死链 ✓」）。

## 报告结构（HTML）

```
🎯 速读卡    （公司一句话 + 3 个关键数字 + 1 个会面抓手）
01 公司快照  （定位/主体/融资/产品/竞品/客户，各带来源）
02 招聘信号  （岗位分布 → 业务重点，带平台链接）
03 风险信号  （事实/依据/对我方 ×2，带来源）
04 会面话题  （3 个可开口话题 + Fit 评估：强/中/弱 徽章 + 理由 + 来源，放在话题之后）
05 社交账号  （微博/抖音/小红书/领英/X… 账号+粉丝+链接按钮）
附录  已检索平台清单（✓/未检索到 + 理由）· 免责声明
```

## 免责声明（社媒 + 招聘区共用，措辞各自贴切）

- 社媒区：`社交账号基于公开检索结果整理，受各平台可见性、登录墙与检索覆盖限制，账号无法保证收录完全，仅供会前参考；未检索到 ≠ 不存在。`
- 招聘区：`招聘信息基于公开平台检索整理，受各平台收录范围、更新时效与地区差异限制，在招岗位无法保证收录完全，仅供会前参考；未检索到 ≠ 当前无招聘。`

## 输出

单文件 HTML（report-template.html 填充），保存 `company-brief-<公司名>.html`，**聊天中输出交付句**（浏览器弹出仅辅助，不是交付标准）。
